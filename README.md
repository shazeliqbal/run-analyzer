Run Analyzer In Java

A Java tool that reads a CSV file and creates per segment stats.
This involves attempts, fails, best time, average time.

## HOW TO RUN ##
Put your log at data/run_log.csv
Run Main.java in IntelliJ

## CSV FORMAT ##
timestamp, runId, segment, event
